# AskAI Backend

This is the modular backend for the AskAI Q&A system.

## Setup

1. Install dependencies: `npm install`
2. Create a `.env` file with your `OPENROUTER_API_KEY`.
3. Start the server: `npm start`
